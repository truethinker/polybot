"""Internal package for the Polymarket BTC 5m slot bot.

This file intentionally kept minimal; it just marks the folder as a package so
imports like `from tool.config import load_config` work reliably in containers.
"""
