py-clob-client==0.20.0
requests==2.32.3
python-dotenv==1.0.1
pytz==2025.1
# web3 6.x pins hexbytes<0.4, which conflicts with current eth-account (hexbytes>=1.2).
# web3 7.x aligns with eth-account and resolves cleanly.
web3==7.14.1
